name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            target: aarch64-apple-darwin
          - os: macos-15-intel
            target: x86_64-apple-darwin
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          target: ${{ matrix.target }}

      - name: Cache Rust builds
        uses: Swatinem/rust-cache@v2

      - name: Read package version
        id: package
        shell: bash
        run: |
          version="$(
            cargo metadata --locked --format-version 1 --no-deps \
              | python3 -c 'import json, sys; print(next(package["version"] for package in json.load(sys.stdin)["packages"] if package["name"] == "mark-cli"))'
          )"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Verify tag matches package version
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          tag_version="${GITHUB_REF_NAME#v}"
          if [ "$tag_version" != "${{ steps.package.outputs.version }}" ]; then
            echo "tag version $tag_version does not match mark-cli version ${{ steps.package.outputs.version }}" >&2
            exit 1
          fi

      - name: Test
        run: cargo test --workspace --all-targets --all-features --locked --target ${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release -p mark-cli --locked --target ${{ matrix.target }}

      - name: Package release asset
        shell: bash
        run: |
          version="${{ steps.package.outputs.version }}"
          package="mark-v${version}-${{ matrix.target }}"
          binary="target/${{ matrix.target }}/release/mark"
          asset="dist/${package}.tar.gz"

          test -x "$binary"
          mkdir -p "dist/$package"
          install -m 755 "$binary" "dist/$package/mark"
          cp README.md LICENSE "dist/$package/"
          tar -czf "$asset" -C dist "$package"

          if command -v shasum >/dev/null 2>&1; then
            (cd dist && shasum -a 256 "${package}.tar.gz" > "${package}.tar.gz.sha256")
          else
            (cd dist && sha256sum "${package}.tar.gz" > "${package}.tar.gz.sha256")
          fi

      - name: Upload release asset
        uses: actions/upload-artifact@v7
        with:
          name: mark-${{ matrix.target }}
          if-no-files-found: error
          path: |
            dist/*.tar.gz
            dist/*.sha256

  publish:
    name: Publish GitHub release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Download release assets
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          version="$(
            sed -n '/^\[workspace.package\]/,/^\[/{s/^version[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p;}' Cargo.toml \
              | head -n 1
          )"
          if [ -z "$version" ]; then
            echo "could not read workspace package version" >&2
            exit 1
          fi

          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            release_tag="$GITHUB_REF_NAME"
            create_tag_args=(--verify-tag)
          else
            release_tag="v$version"
            create_tag_args=(--target "$GITHUB_SHA")
          fi

          tag_version="${release_tag#v}"
          if [ "$tag_version" != "$version" ]; then
            echo "release tag $release_tag does not match mark-cli version $version" >&2
            exit 1
          fi

          cat > release-notes.md <<EOF
          Release $release_tag.

          Install with curl:

          \`\`\`sh
          curl -fsSL https://raw.githubusercontent.com/phongndo/mark/main/scripts/install.sh | sh
          \`\`\`
          EOF

          if gh release view "$release_tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$release_tag" dist/*.tar.gz dist/*.sha256 \
              --repo "$GITHUB_REPOSITORY" \
              --clobber
            gh release edit "$release_tag" \
              --repo "$GITHUB_REPOSITORY" \
              --latest
          else
            gh release create "$release_tag" dist/*.tar.gz dist/*.sha256 \
              --repo "$GITHUB_REPOSITORY" \
              "${create_tag_args[@]}" \
              --latest \
              --title "mark $version" \
              --notes-file release-notes.md
          fi
